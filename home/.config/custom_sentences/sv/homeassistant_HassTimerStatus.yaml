language: "sv"
intents:
  HassTimerStatus:
    data:
      - sentences:
          - "timer[s] status"
          - "status på[ den| min] timer[s]"
          - "[hur mycket ]tid[ är] kvar på[ den| min] timer[s]"
          - "<timer_start> timer status"
          - "status på[ den| min] <timer_start> timer"
          - "[hur mycket ]tid[ är] kvar på[ den| min] <timer_start> timer"
          - "{area} timer status"
          - "status på[ den| min] {area} timer[s]"
          - "status på[ den| min] timer[s] i <area>"
          - "[hur mycket ]tid[ är] kvar på[ den| min] {area} timer"
          - "[hur mycket ]tid[ är] kvar på[ den| min] timer[s] i <area>"
          - "{timer_name:name} timer status"
          - "status på[ den| min] {timer_name:name} timer[s]"
          - "[hur mycket ]tid[ är] kvar på[ den| min] {timer_name:name} timer"

